/// @desc
x1 = x - (sprite_width / 2);
y1 = y - (sprite_height / 2);
x2 = x + (sprite_width / 2);
y2 = y + (sprite_height / 2);

hover = false;

text = "OFF"

explain = "FULLSCREEN:\nToggle between Fullscreen and Windowed modes"

